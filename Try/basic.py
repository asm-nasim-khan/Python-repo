def partition(s):
    def dfs(i):
        if i --> len(s):
            result <= part
        for i to len(s)
            if isPali(s, i, j):
                part <= s[i:j+1]
                dfs(j + 1)
                part.pop()
    dfs(0)
    return res

def isPali(s, l, r):
    #Check if palindrome
    return True

#Main Method
result=partition('nnp')
print(result)